# MovieRecommendation
Movie Recommendation System is a Python-based application that suggests movies to users based on genre and rating. It also allows users to add new movies, making the system dynamic and interactive. The project uses a dataset stored in a CSV file and applies filtering and sorting techniques to generate recommendations.
